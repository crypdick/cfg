target_branch = "main"
setup = ["uv sync --all-groups --locked"]
pre_merge = [
    "uv sync --all-groups --locked",
    "uvx prek run --all-files",
]
post_merge = [
    "uv sync --all-groups --locked",
    "uvx prek run --all-files",
]
teardown = []
